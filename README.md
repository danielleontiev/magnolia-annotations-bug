# In the wild tapir example

```bash
sbt ";clean;tapir/compile"
```

# .baseClass examples

```bash
sbt ";clean;baseclass/compile"
```

# name issue in magnolia

```bash
sbt ";clean;names/run"
```

# pattern matching issue

```bash
sbt ";clean;pattern-matching/run"
```